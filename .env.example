# Server-only database connection. Never expose this as NEXT_PUBLIC_*.
DATABASE_URL=
DATABASE_URL_UNPOOLED=

# Neon Managed Better Auth
NEON_AUTH_BASE_URL=
NEON_AUTH_COOKIE_SECRET=

# Optional future model provider. Zeus M1 boots without it.
OPENROUTER_API_KEY=

# Optional deployment origin used by origin checks.
ZEUS_PUBLIC_ORIGIN=http://localhost:3000
